<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Connexion - Chat en Temps Réel</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div id="login" style="display: flex; flex-direction: column; align-items: center; margin-top: 50px;">
        <h2>Connexion</h2>
    <form id="loginForm" autocomplete="off" method="POST" action="/login">
            <input type="text" id="pseudo" name="pseudo" placeholder="Votre pseudo" required autofocus>
            <input type="password" id="password" name="password" placeholder="Mot de passe" required>
            <button type="submit">Se connecter</button>
        </form>
    {% if error %}
    <div id="loginError" style="color:red;">{{ error }}</div>
    {% else %}
    <div id="loginError" style="color:red; display:none;"></div>
    {% endif %}
        <a href="/register">Créer un compte</a>
    </div>
    <script src="/chat.js"></script>
</body>
</html>